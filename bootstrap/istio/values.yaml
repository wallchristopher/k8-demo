kiali-operator:
  cr:
    create: true
    namespace: istio-system
    spec:
      external_services:
        prometheus:
          url: http://monitoring-kube-prometheus-prometheus.monitoring:9090
        grafana:
          url: http://monitoring-kube-prometheus-grafana.monitoring
