kube-prometheus-stack:
  prometheus:
    thanosService:
      enabled: true
    thanosServiceMonitor:
      enabled: true
    prometheusSpec:
      serviceMonitorSelectorNilUsesHelmValues: false
      replicas: 2
      thanos:
        ObjectStorageConfig:
          type: FILESYSEM
          config:
            directory: /tmp
